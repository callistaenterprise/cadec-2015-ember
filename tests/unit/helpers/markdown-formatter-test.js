import {
  markdownFormatter
} from 'cadec-2015-ember/helpers/markdown-formatter';

module('MarkdownFormatterHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = markdownFormatter(42);
  ok(result);
});
