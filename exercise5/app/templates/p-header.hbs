<!-- Fixed navbar -->
<div class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Cadec Blog</a>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li>{{#link-to 'posts'}}Posts{{/link-to}}</li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if session.isLoggedIn}}
          <li><p class="navbar-text"> Signed in as {{session.username}} </p></li>
          <li>{{#link-to 'logoff'}}<i class="fa fa-power-off"></i> Log off{{/link-to}}</li>
        {{else}}
          <li><p class="navbar-text">{{session.username}}</p></li>
          <li>{{#link-to 'login'}}<i class="fa fa-power-on"></i> Log In{{/link-to}}</li>
        {{/if}}

      </ul>
    </div>
    <!--/.nav-collapse -->
  </div>
</div>
