import {
  markdownFormatter
} from 'blogs-app/helpers/markdown-formatter';

module('MarkdownFormatterHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = markdownFormatter(42);
  ok(result);
});
