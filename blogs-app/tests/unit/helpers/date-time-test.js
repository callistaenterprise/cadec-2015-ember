import {
  dateTime
} from 'blogs-app/helpers/date-time';

module('DateTimeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = dateTime(42);
  ok(result);
});
